/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.sportsdirect.com', 'images.unsplash.com', 'th.bing.com', '', 'cdn7.dissolve.com', 'kapiva-in-nextjs.pages.dev'],
    },
};

export default nextConfig;
